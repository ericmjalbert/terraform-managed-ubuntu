# Custom scripts in ~/.local/bin/ managed via the dotfiles provider.

# resource "dotfile" "gh_logs" {
#   path        = "${var.home}/.local/bin/gh-logs"
#   content     = file("${path.module}/files/gh-logs")
#   permissions = "0755"
# }
